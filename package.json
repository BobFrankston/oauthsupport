{
  "name": "@bobfrankston/oauthsupport",
  "version": "1.0.29",
  "description": "Universal OAuth token manager supporting any OAuth provider with automatic token refresh and caching",
  "main": "index.js",
  "types": "index.d.ts",
  "type": "module",
  "files": [
    "*.js",
    "*.d.ts",
    "*.html",
    "README*.md",
    "!*.test.js",
    "!lifetime-examples.js",
    "!runner.js",
    "!OAuthTokenManager.example.js"
  ],
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc",
    "prerelease:local": "git add -A && (git diff-index --quiet HEAD || git commit -m \"Pre-release commit\")",
    "preversion": "npm run check && npm run build && git add -A",
    "release": "npm whoami && npm run prerelease:local && npm version patch && npm publish --access public",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bobfrankston/oauthsupport.git"
  },
  "keywords": [
    "oauth",
    "oauth2",
    "authentication",
    "token",
    "google",
    "github",
    "microsoft",
    "refresh-token",
    "authorization"
  ],
  "author": "Bob Frankston",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
